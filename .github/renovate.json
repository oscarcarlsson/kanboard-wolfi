{
  "extends": ["config:base"],
  "regexManagers": [
    {
      "fileMatch": ["^VERSION$"],
      "matchStrings": ["^(v.*)$"],
      "datasourceTemplate": "github-releases",
      "depNameTemplate": "kanboard/kanboard",
      "versioningTemplate": "semver"
    }
  ]
}
